// cargo build: just build
// cargo run: build and run
// cargo check: verify errors without a binary being created
// cargo build --release: default build is for debug.

fn main() {
    println!("Hello, cargo!");
}
